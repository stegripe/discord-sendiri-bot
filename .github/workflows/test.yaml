name: Lint code and compile setup script

on:
  push:
    branches:
      - "**"
  pull_request:
    branches:
      - "**"

jobs:
  test:
    uses: stegripe/workflows/.github/workflows/pnpm-lint-and-test.yaml@fcfd6bae08c7afa0a213fae4584a8667f0214688 # main
    secrets: inherit
